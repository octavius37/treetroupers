# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example
# Committed on purpose: contains only the fixed, published demo keys that the
# local Supabase stack uses on every machine. No real credentials.
!.env.local.example

# Supabase CLI local state (containers, logs, temp files)
supabase/.branches
supabase/.temp

# Playwright MCP artifacts
.playwright-mcp/

# Database
**/*/pglite-data
prisma/pglite-data*.zip
pgliteHealthz
prisma/schema_old.prisma
prisma/enums
prisma/client
.vercel
.mcp.json
